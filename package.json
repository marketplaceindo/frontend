{
  "name": "marketindonesia-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Nuxt 4 SSR (situs tenant) + dashboard MarketIndonesia. Schema/DTO API diimpor dari @marketplaceindo/shared — dilarang didefinisikan ulang di sini.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@marketplaceindo/shared": "0.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
